//! Lifecycle module: Tauri builder hooks (run-event, window-event, setup).
//!
//! File-named module per Rust 2018+ idiom (`nv-rust-best-practices` rule #2 —
//! no `mod.rs`). Submodules live in `src/lifecycle/<name>.rs`.

pub mod run_event;
