{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "event-settings-changed",
  "description": "Allow every window to listen for settings broadcasts emitted from save_settings.",
  "windows": ["*"],
  "permissions": ["core:event:default"]
}
